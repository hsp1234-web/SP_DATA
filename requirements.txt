pandas
duckdb
requests
PyYAML
yfinance
openpyxl
numpy
python-json-logger
# Ensure finlab is listed if direct finlab connector is developed and tested
# finlab
# Ensure specific versions if needed, e.g., pandas==1.5.3
# Add other direct dependencies of your project here.
# Dependencies of these packages (like pytz for pandas) will be handled by pip.
